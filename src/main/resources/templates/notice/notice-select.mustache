{{>layout/header}}
<div id="board_banner" class="notice">
    <h1 class="ir_pm">새소식</h1>
</div>

<div id="write">
    <div class="container">
        <div class="write_box">
            <form>
                <div class="form-group">
                    <label for="title">제목</label>
                    <input type="text" class="form-control title" placeholder="제목을 입력하세요" value="{{notice.title}}" readonly>
                </div>
                <div class="form-group" >
                    <label for="author">작성자</label>
                    <input type="text" class="form-control author" value="{{notice.author}}" readonly>
                </div>
                <div class="form-group">
                    <label for="content">내용</label>
                    <div class="content">{{{notice.content}}}</div>
                </div>
                <input type="submit" id="btn-move-update" name="{{notice.id}}" value="수정" />
            </form>
        </div>
    </div>
</div>

{{>layout/footer}}