{{>layout/header}}


<div id="login">
    <div class="container clearfix">
        <div class="login">
            <div class="login-word">
                <p>EVERY WALL</p>
                <p>IS A DOOR</p>
                <p>모든 벽은 문이다.</p>
            </div>
            <form>
                <div class="form-group">
                    <input id="id" type="text" class="form-control title" placeholder="아이디">
                </div>
                <div class="form-group">
                    <input id="password" type="password" class="form-control title" placeholder="비밀번호">
                </div>
                <a type="submit" id="btn-sign-in" class="btn">로그인</a>
                <a type="submit" id="btn-sign-up" class="btn mt5" onclick="location.href='/signup'">회원가입</a>
            </form>
        </div>
    </div>
</div>
{{>layout/footer}}